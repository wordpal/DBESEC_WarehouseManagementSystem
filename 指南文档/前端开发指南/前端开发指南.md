🎯 前端开发指南

一、项目启动步骤

环境要求



浏览器：Chrome 90+ / Firefox 88+



编辑器：VS Code / WebStorm



Node.js：可选，用于本地开发服务器



获取API接口



text

基础URL：http://localhost:8080/WarehouseManagementSystem

API前缀：/api/

所有请求返回JSON格式

开发工具推荐



Postman：API测试



ECharts：数据可视化图表



LayUI：前端UI框架（已集成）



二、页面与API对应表

页面模块	主要API接口	HTTP方法	功能说明

登录页	前端模拟	-	简单用户名/密码验证

仪表盘	/api/dashboard?action=summary	GET	数据概览卡片

/api/dashboard?action=warehouseDistribution	GET	仓库库存分布图

/api/dashboard?action=lowStockAlerts	GET	低库存预警列表

产品管理	/api/product?action=getByPage	GET	产品列表（分页）

/api/product?action=search	GET	产品搜索

/api/product?action=getById	GET	产品详情

仓库管理	/api/warehouse?action=getAll	GET	仓库列表

/api/warehouse?action=getStats	GET	仓库统计

入库管理	/api/inbound?action=getAll	GET	入库单列表

/api/inbound?action=smartInbound	POST	智能入库

出库管理	/api/outbound?action=getAll	GET	出库单列表

/api/outbound?action=fifoOutbound	POST	FIFO出库

库存查询	/api/inventory?action=getAll	GET	库存列表

/api/inventory?action=recommendLocation	GET	智能货位推荐

三、API调用示例

javascript

// 1. 获取仪表盘数据

async function loadDashboard() {

&nbsp;   const response = await fetch('/WarehouseManagementSystem/api/dashboard?action=summary');

&nbsp;   const result = await response.json();

&nbsp;   

&nbsp;   if (result.code === 200) {

&nbsp;       const data = result.data;

&nbsp;       // 更新页面数据

&nbsp;       document.getElementById('totalProducts').textContent = data.totalProducts;

&nbsp;       document.getElementById('totalWarehouses').textContent = data.totalWarehouses;

&nbsp;       document.getElementById('inventoryValue').textContent = formatCurrency(data.totalInventoryValue);

&nbsp;   }

}



// 2. 获取产品列表（分页）

async function loadProducts(page = 1, pageSize = 10) {

&nbsp;   const url = `/WarehouseManagementSystem/api/product?action=getByPage\&pageNum=${page}\&pageSize=${pageSize}`;

&nbsp;   const response = await fetch(url);

&nbsp;   const result = await response.json();

&nbsp;   

&nbsp;   if (result.code === 200) {

&nbsp;       renderProductTable(result.data.list);

&nbsp;       updatePagination(result.data.total, page, pageSize);

&nbsp;   }

}



// 3. 智能入库操作

async function smartInbound(productId, quantity, operator) {

&nbsp;   const params = new URLSearchParams({

&nbsp;       action: 'smartInbound',

&nbsp;       productId,

&nbsp;       quantity,

&nbsp;       operator

&nbsp;   });

&nbsp;   

&nbsp;   const response = await fetch(`/WarehouseManagementSystem/api/inbound?${params}`, {

&nbsp;       method: 'POST'

&nbsp;   });

&nbsp;   

&nbsp;   return await response.json();

}

四、统一响应处理

javascript

// 统一API调用函数

async function callApi(url, options = {}) {

&nbsp;   try {

&nbsp;       const response = await fetch(url, options);

&nbsp;       const result = await response.json();

&nbsp;       

&nbsp;       if (result.code === 200) {

&nbsp;           return { success: true, data: result.data };

&nbsp;       } else {

&nbsp;           console.error('API错误:', result.msg);

&nbsp;           showError(result.msg);

&nbsp;           return { success: false, error: result.msg };

&nbsp;       }

&nbsp;   } catch (error) {

&nbsp;       console.error('网络错误:', error);

&nbsp;       showError('网络连接失败，请检查服务器状态');

&nbsp;       return { success: false, error: error.message };

&nbsp;   }

}



// 使用示例

const result = await callApi('/api/product?action=getAll');

if (result.success) {

&nbsp;   // 处理成功数据

&nbsp;   console.log(result.data);

}

五、页面布局建议

text

首页 (index.html)

├── 顶部导航栏

├── 左侧菜单

│   ├── 📊 仪表盘

│   ├── 📦 产品管理

│   ├── 🏭 仓库管理

│   ├── 📥 入库管理

│   ├── 📤 出库管理

│   └── 📊 库存查询

└── 主内容区



仪表盘页面 (dashboard.html)

├── 数据卡片（4个）

│   ├── 总产品数

│   ├── 总仓库数

│   ├── 库存总价值

│   └── 低库存预警

├── ECharts图表

│   ├── 仓库库存分布（饼图）

│   └── 出入库趋势（折线图）

└── 低库存预警表格

六、开发进度建议

第1周：



完成登录页、仪表盘页面



实现基础布局和导航



完成产品管理列表页



第2周：



完成入库、出库管理页面



实现ECharts图表集成



完成所有页面基础功能



第3周：



前后端联调测试



界面优化和响应式适配



最终测试和Bug修复



七、核心功能实现要点

智能货位推荐展示（核心亮点）



在入库页面显示推荐货位



高亮显示推荐理由和评分



低库存预警（核心亮点）



红色高亮显示库存不足的产品



提供补货建议数量



FIFO出库策略



显示按入库时间排序的库存批次



自动计算可出库数量



八、立即开始的任务

✅ 今天可以开始：



创建项目目录结构



编写基础HTML/CSS框架



实现仪表盘页面布局



测试第一个API调用



📞 需要协调的事项：



确认所有API接口可用



讨论ECharts图表具体样式



确定页面交互细节

